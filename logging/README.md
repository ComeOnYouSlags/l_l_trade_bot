# logging

This package contains functions for logging

### CreateLogFile

```go
func CreateLogFile(logDir string) (string, error)
```
args:           string Directory Path to put the log file in

r.type:         string Path/To/Log/File, error message

Returns a formatted string of converted milliseconds timestamp